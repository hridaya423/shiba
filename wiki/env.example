# MediaWiki Configuration
SITE_NAME=Shiba Wiki
SITE_SERVER=http://localhost:8080
ADMIN_USER=admin
ADMIN_PASS=adminpass
ADMIN_EMAIL=admin@example.com

# External Database Configuration (Coolify/Remote MySQL)
# Uncomment and configure these to use an external database
# DB_HOST=your-coolify-server.com
# DB_PORT=3306
# DB_USER=wikiuser
# DB_PASSWORD=your-secure-password
# DB_NAME=wikidb

# Local Database Configuration (default - uses Docker MySQL)
# These are used when no external DB is configured
DB_HOST=database
DB_PORT=3306
DB_USER=wikiuser
DB_PASSWORD=wikipass
DB_NAME=wikidb

# MySQL Root Password (for local database only)
MYSQL_ROOT_PASSWORD=rootpass
